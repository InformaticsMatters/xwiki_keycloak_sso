Bouncy castle classes needed by keycloak (in TOMCAT_HOME/lib) clash with those in xwiki (in TOMCAT_HOME/ROOT/WEB-INF/lib).
bcpkix-jdk15on-1.50.jar	bcprov-jdk15on-1.50.jar have been replaced with version 1.5.3 to match those in xwiki.
